hi chat I want to make a mac os app for wall papers. the feature is auto set wall papers for a interval, like per hour, per day, or customized, and day and night maybe

Some features: 

- Get image from unsplash havent shown before
- Track what we have shown in a grid and allow to download previous in a grid and maybe switch back
- set a customize topics 

- I want it to be architecture focused
- able to display architecture name
- call ollama using a customized model and use ollama search tool if avaliable, generate a one sentance inrtudcution.
- simple yet easy ui for settings categorized by types in a top tab bar, use swift ui with ios 26 liquid glass. use native as possible. 
- for example if we are visiting le corbusier’s church, show image. 
- Have a desktop widget to show the name and the one sentance introduction. 
- Modular, easy to intergrate other components and function one day like switch from architecture to others. 
- Some simple filter like by color or aspect ratio, and maybe we can set two different set based on dark mode 
- some simple image costomization, like their fit mode and crop
- add a optional option to do buildings based on user location, like shown buildings nearby, same building can be duplicated but user can choose if they wants to see the same image again. I think this can be done in main agent search based on user location like famouse architecture building nearby, and do a list, and use mapkit to calculate distance maybe. User can turn it off for just random buildings around the world. idk maybe we can have a better method. 
- some cache management settings to auto clean and only keep reference link and text into the app storage instead of full image
- Pre download and generate for the next few image and pre-cache it
- Make sure it intergrate with macos well and use all APIs and stuff, handle multi display, handel restart, no too much ram usage and no overflow
- instead of put the text and author etc in the widget also have an option for user to put it somewhere burn into the image on ether corner with customized margin and customized text font and color and size, (some basic stuff) with a ui to preview with performance management so don’t consumes too much GPU. 
- Good UI. 